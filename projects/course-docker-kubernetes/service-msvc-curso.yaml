apiVersion: v1
kind: Service
metadata:
  name: msvc-curso
spec:
  ports:
  - port: 8082
    protocol: TCP
    targetPort: 8082
  selector:
    app: msvc-curso
  sessionAffinity: None
  type: LoadBalancer

