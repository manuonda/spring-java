server:
  port: 8085

spring:
  application:
    name: service-email
  kafka:
    topic:
      envio_email_producto: envio_email_producto
    consumer:
      bootstrap-servers: localhost:29092
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: '*' # Esto permite cualquier clase en la deserializaci�n
  mail:
    host: email-smtp.us-east-1.amazonaws.com
    port: 587
    username: AKIASE4MOIVA44RF3LGY
    password: BMVbxrmdeA1rS1cF0M3D8Rkp/25PiM+zCMpNvkkmNA1m
    properties:
      mail:
        transport:
          protocol: smtp
        smtp:
          port: 587
          auth: true
          starttls:
            enable : true
            required: true




# === configuration eureka discover client
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

# ====  actuator
management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: '*' # para exponer todos los endpoints de Actuator





