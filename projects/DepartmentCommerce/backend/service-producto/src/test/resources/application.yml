spring:
  application:
    name: service-producto
  datasource:
    url: jdbc:postgresql://localhost:5432/db_commerce
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

